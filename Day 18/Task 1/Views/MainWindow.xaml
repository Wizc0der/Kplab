<Window x:Class="Task_1.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Task_1.ViewModels"
        xmlns:local="clr-namespace:Task_1.Views"
        mc:Ignorable="d"
        Title="Система управления бронированием" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <!-- Ресурсы и стили -->
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <local:StatusColorConverter x:Key="StatusColor"/>

        <!-- Кисти -->
        <SolidColorBrush x:Key="Accent" Color="#4A90E2"/>
        <SolidColorBrush x:Key="AccentDark" Color="#357ABD"/>
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Border" Color="#E9ECEF"/>
        <SolidColorBrush x:Key="TextMain" Color="#212529"/>
        <SolidColorBrush x:Key="TextLight" Color="#6C757D"/>
        <SolidColorBrush x:Key="BgHover" Color="#F1F3F5"/>

        <!-- Глобальные стили -->
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma, Geneva, Verdana, sans-serif"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer Margin="{TemplateBinding Padding}" x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="Background" Value="{StaticResource Surface}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard Duration="0:0:0.15">
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="0.97" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.97" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentDark}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{StaticResource Border}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="14"
                                Margin="2"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E8F4FD"/>
                                <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="ToolBarTray">
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style TargetType="ToolBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <!-- Горячие клавиши -->
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding BookCommand}"/>
        <KeyBinding Key="E" Modifiers="Ctrl" Command="{Binding EditCommand}"/>
        <KeyBinding Key="D" Modifiers="Ctrl" Command="{Binding CancelCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <!-- Меню -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл" FontSize="13">
                <MenuItem Header="Выход" Click="ExitMenuItem_Click" FontSize="13"/>
            </MenuItem>
            <MenuItem Header="Номера" FontSize="13"/>
            <MenuItem Header="Бронирование" FontSize="13"/>
            <MenuItem Header="Настройки" FontSize="13"/>
        </Menu>

        <!-- Панель инструментов -->
        <ToolBarTray DockPanel.Dock="Top" Height="40" Background="Transparent">
            <ToolBar>
                <Button Content="Забронировать" Command="{Binding BookCommand}"/>
                <Button Content="Редактировать" Command="{Binding EditCommand}"/>
                <Button Content="Отменить" Command="{Binding CancelCommand}"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Основной контент -->
        <Grid Margin="20" DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая часть: Список номеров -->
            <Border Background="{StaticResource Surface}" CornerRadius="14" Padding="4" Margin="0,0,15,0">
                <ListView ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="RoomCard"
                                    Background="{StaticResource Surface}"
                                    CornerRadius="12"
                                    Padding="14"
                                    Margin="2"
                                    BorderThickness="1"
                                    BorderBrush="{StaticResource Border}"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>

                                <Border.Triggers>
                                    <!-- Анимация при загрузке элемента -->
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.9" To="1" Duration="0:0:0.4"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.9" To="1" Duration="0:0:0.4"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>

                                    <!-- Пульсация для забронированных -->
                                    <DataTrigger Binding="{Binding Status}" Value="Booked">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="PulseBorder">
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="(Border.BorderThickness)" To="3" Duration="0:0:0.3" AutoReverse="True"/>
                                                    <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                                    To="#FFB71C1C" Duration="0:0:0.3" AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <StopStoryboard BeginStoryboardName="PulseBorder"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Border.Triggers>

                                <StackPanel>
                                    <TextBlock Text="{Binding Number, StringFormat='{}Комната №{0}'}"
                                               FontWeight="SemiBold" FontSize="15"/>
                                    <TextBlock Text="{Binding PricePerNight, StringFormat='{}{0:C} / ночь'}"
                                               FontSize="13" Foreground="{StaticResource TextLight}" Margin="0,2"/>
                                    <Grid Margin="0,6,0,0">
                                        <Ellipse Width="8" Height="8" Fill="{Binding Status, Converter={StaticResource StatusColor}}"/>
                                        <TextBlock Text="{Binding Status}" Margin="14,0,0,0" FontSize="12"
                                                   Foreground="{StaticResource TextLight}"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <!-- Правая часть: Форма -->
            <Border Grid.Column="1" Background="{StaticResource Surface}" CornerRadius="14" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Форма бронирования -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="Бронирование" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>

                        <TextBox Text="{Binding GuestName, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="14" Margin="0,0,0,8"
                                 Tag="ФИО Гостя"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <DatePicker Grid.Column="0" SelectedDate="{Binding CheckIn, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,4,0"/>
                            <DatePicker Grid.Column="1" SelectedDate="{Binding CheckOut, UpdateSourceTrigger=PropertyChanged}" Margin="4,0,0,0"/>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Content="Сохранить" Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
                            <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Детали выбранного номера -->
                    <Border Grid.Row="1" Background="{StaticResource BgHover}" CornerRadius="10" Padding="16" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="Детали номера" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="Статус:" FontWeight="SemiBold" Width="80"/>
                                <Ellipse Width="10" Height="10" Fill="{Binding SelectedRoom.Status, Converter={StaticResource StatusColor}}" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding SelectedRoom.Status}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="Цена:" FontWeight="SemiBold" Width="80"/>
                                <TextBlock Text="{Binding SelectedRoom.PricePerNight, StringFormat={}{0:C}}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Гость:" FontWeight="SemiBold" Width="80"/>
                                <TextBlock Text="{Binding GuestName, FallbackValue='Нет брони'}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Кнопки действий -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Удалить бронь" Command="{Binding DeleteCommand}" 
                                Visibility="{Binding IsDeleteVisible, Converter={StaticResource BoolToVis}}"
                                Margin="0,0,10,0"/>
                        <Button Content="Обновить" Command="{Binding RefreshCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>